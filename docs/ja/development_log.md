# 開発ログ

## Phase 6 - GUIステータスモニタ

### 概要

PySide6 GUIクライアントに、構造化されたステータス表示欄を追加しました。

### 追加内容

- `parse_status_response()` ヘルパー。
- Status Monitorグループボックス。
- state、temperature、humidity、last update の専用表示欄。
- `STATUS` 応答からの表示更新処理。
- 不正な `STATUS` 応答に対する警告ログ。

### メモ

- 既存の送信ログ・受信ログは残しています。
- TCPサーバ側のプロトコルは変更していません。
- 自動ポーリングは追加していません。

## Phase 5 - PySide6 GUI TCP クライアント

### 概要

PySide6 を使った GUI TCP クライアントを追加しました。

### 追加内容

- `client/python_gui/tcp_gui_client.py`。
- `client/python_gui/requirements.txt`。
- `client/python_gui/README.md`。
- host と port の入力欄。
- Connect / Disconnect ボタン。
- 既存TCPサーバプロトコル用のコマンドボタン。
- 送信ログと受信ログの表示。
- 接続状態表示。
- ワーカースレッドによる socket 通信処理。

### メモ

- C言語TCPサーバ側のプロトコルは変更していません。
- 既存のCLIクライアントは `client/python/` 配下に残しています。

## Phase 4 - Python TCP クライアント

### 概要

Windows側から通信確認できる Python 製 CLI クライアントを追加しました。

### 追加内容

- `client/python/tcp_client.py`。
- `--host` と `--port` のコマンドライン引数。
- 既存TCPサーバプロトコルに対応した対話入力。
- サーバ応答の標準出力表示。
- `Ctrl+C` での安全な終了処理。
- 分かりやすい接続エラーメッセージ。
- README と実装メモの更新。

### メモ

- クライアントはPython標準ライブラリのみを使用します。
- このフェーズではGUIは追加していません。
- TCPサーバ側のプロトコルは変更していません。

## Phase 3 - 基本 TCP サーバ

### 概要

C 言語で最初に動作するシングルクライアント TCP サーバを実装しました。

### 追加内容

- TCP ポート `5000` で待ち受ける基本 TCP サーバ。
- `PING`、`GET_STATUS`、`START`、`STOP`、`RESET`、`QUIT` の行単位コマンド処理。
- `STOP` と `RUN` の内部状態管理。
- 起動、接続、受信、送信、終了を標準出力へ表示するログ。
- ビルド、実行、手動テスト手順。
- 実装メモ。
- `CHANGELOG.md` と `CONTRIBUTING.md`。

### メモ

- 複数クライアント対応と GUI クライアントは今後のフェーズで扱います。
- CSV ログ、設定ファイル、CAN 連携、テストは Phase 3 の対象外です。

## Phase 2.5 - リポジトリ公開準備

### 概要

初回の公開 GitHub リポジトリとして見せられる状態に整えました。

### 追加内容

- 言語別ドキュメントディレクトリ。
- Ubuntu セットアップスクリプト。
- GitHub 閲覧者向け README。
- 公開用ロードマップ。
- 初期 Git リポジトリ構成。

### メモ

- このフェーズではソケット実装は追加していません。
- `docs/en/` の英語版は実装仕様として扱います。
- `docs/ja/` の日本語版は公開説明と面接準備用として扱います。

## Phase 2 - 設計書作成

### 概要

TCP/IP 通信制御システムの基本設計書を作成しました。

### 追加内容

- システム概要。
- 通信プロトコル仕様。
- 通信シーケンス例。
- 状態遷移設計。
- 日本語公開用ドキュメント。
- README のロードマップ更新。

### メモ

- このフェーズではソケット実装は追加していません。
- 通信プロトコルは行単位の UTF-8 テキストです。
- Phase 3 では `docs/en/protocol_spec.md` に従ってサーバを実装します。

## Phase 1 - プロジェクトの土台

### 概要

初期プロジェクト構成を作成しました。

### 追加内容

- C17 用の最小 CMake 設定。
- README。
- 初期ドキュメント。
- `.gitignore`。
- MIT ライセンス。

### メモ

ソケット通信の実装は意図的に後続フェーズへ回しました。
